## Ounglang - The Programming Language for SEALs in the SEA

To build the interpreter, just simply run...
```sh
cd interpreter
```
And then use your ```gcc``` or ```clang``` to compile the C program
```sh
gcc oung.c -o oung
```

